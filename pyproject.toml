[project]
name = "pytorch-custom-cuda-ops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9, <3.11"
dependencies = [
    "torch==2.8.0",
    "ninja>=1.13.0",
    "numpy>=2.0.2",
]
